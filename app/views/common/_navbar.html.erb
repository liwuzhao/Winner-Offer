<nav class="navbar navbar-default navbar-fixed-top " role="navigation" >
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <a class="navbar-brand" href="/"><img src='<%= image_url 'logo.png' %>' >Winner&nbsp;&nbsp;&nbsp;&nbsp;</a>
            <a class="navbar-brand" href="/jobs">工作</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">


            <ul class="nav navbar-nav navbar-right">
              <% if !current_user %>
                <li><a href="#" data-toggle="modal" data-target="#signup-modal"><i class="fa fa-user fa-lg" aria-hidden="true"></i>&nbsp;&nbsp;注册</a></li>
                <li><a href="#" data-toggle="modal" data-target="#login-modal"><i class="fa fa-sign-in fa-lg" aria-hidden="true"></i>&nbsp;&nbsp;登录</a></li>
              <% else %>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="fa fa-user-circle-o fa-lg" aria-hidden="true"></i>
                        <%= current_user.display_name %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                      <% if current_user.admin? %>
                          <li><%= link_to("<span class='fa fa-home fa-lg'></span>管理员面板".html_safe, admin_jobs_path) %></li>
                      <% end %>
                          <li><%= link_to("<span class='fa fa-heart fa-fa-lg'></span> 我的收藏".html_safe, account_jobfs_path) %></li>
                          <li><%= link_to("<span class='fa fa-file-text fa-fa-2x'></span>  投递历史".html_safe, account_jobs_path) %></li>
                          <li><%= link_to("<span class='fa fa-database fa-fa-2x'></span>  个人中心".html_safe, edit_account_user_path(current_user) )%></li>
                          <li class="divider"> </li>
                          <li><%= link_to("<span class='fa fa-sign-out fa-lg'></span> 退出".html_safe, destroy_user_session_path,  method: :delete) %></li>
                    </ul>
                </li>
              <% end %>
            </ul>

            <ul class="nav navbar-nav navbar-right">
              <li>
                  <div class="form-group form-inline search-bar">
                      <%= render :partial => "jobs/search_bar"  %>
                  </div>
              </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>

<!-- 登录弹窗 -->

<div class="modal fade" id="signup-modal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true" style="display: none;">
  <div class="modal-dialog">
    <div class="loginmodal-container">
      <div class="modal-header">
          <h2 class="text-center">注册</h2>
      </div>
      <div class="modal-body">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
                               :html => {:id => "sign_up_user"},
                               :format => :json,
                               :remote => true ) do |f| %>
          <%= devise_error_messages! %>

          <div class="input-group">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-envelope"></span>
            </span>
            <%= f.email_field :email, autofocus: true, required: true, class: "form-control", placeholder: "邮箱" %>
          </div>

          <br>

          <div class="input-group">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-lock"></span>
            </span>
            <%= f.password_field :password, autocomplete: "off", required: true,minlength: 8, class: "form-control", placeholder: "密码至少8个字符" %>
          </div>

          <br>

          <div class="input-group">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-lock"></span>
            </span>
            <%= f.password_field :password_confirmation, autocomplete: "off", required: true, minlength: 8, class: "form-control", placeholder: "确认密码" %>
          </div>

          <div class="form-actions">
            <br>
            <%= f.submit "Sign up", class: 'btn btn-success' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


<div class="modal fade" id="login-modal" role="dialog">
  <div class="modal-dialog">
    <div class="loginmodal-container">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h2 class="text-center">登录</h2>
      </div>
      <div class="modal-body">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name),
                               :html => {:id => "log_in_user"},
                               :format => :json,
                               :remote => true ) do |f| %>
          <div class="input-group">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-envelope"></span>
            </span>
            <%= f.email_field :email, autofocus: true, required: true, class: "form-control", placeholder:"登录邮箱" %>
          </div>

          <br>

          <div class="input-group">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-lock"></span>
            </span>
            <%= f.password_field :password, autocomplete: "off", required: true, class: "form-control", placeholder: "密码" %>
          </div>

          <% if devise_mapping.rememberable? -%>
          <div class="checkbox col-md-12" style="padding-left:1.5em;">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
          <% end -%>

          <div class="form-actions">
            <br>
            <%= f.submit "Log in", class: 'btn btn-success', id: 'login_btn' %>
          </div>
        <% end %>
      </div>


    </div>
  </div>
</div>

<div class="modal fade" id="forgot-modal" role="dialog">
    <div class="modal-dialog">
        <div class="loginmodal-container">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h2>Forgot your password?</h2>
            </div>
            <div class="modal-body">
                <%= form_for(resource, as: resource_name, url: password_path(resource_name),
                                                :html => {:id => "forgot_password_user"},
                                                :format => :json,
                                                :remote => true ) do |f| %>
                    <%= devise_error_messages! %>

                    <div class="input-group">
                      <span class="input-group-addon">
                        <span class="glyphicon glyphicon-envelope"></span>
                      </span>
                      <%= f.email_field :email, autofocus: true, required:true, class: "form-control", placeholder:"Email" %>
                    </div>

                  <div class="form-actions">
                    <br>
                    <%= f.submit "Send me reset password instructions", class: 'btn btn-success' %>
                  </div>
                <% end %>

            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-danger pull-left" data-dismiss="modal"><span class="glyphicon glyphicon-remove"></span> Cancel</button>
                <div class="links pull-right">
                    <button class="btn btn-info btn-small" data-toggle="modal" data-target="#login-modal" data-dismiss='modal'>Log in</button><br />
                    <br>
                    <div class="center-block">
                    <button class="btn btn-info btn-small" data-toggle="modal" data-target="#signup-modal" data-dismiss='modal'>Sign up</button><br />
                  </div>
                </div>
            </div>
        </div>
    </div>
</div>
